import React from "react";

const ProductList = () => {
  // get data from API
  return (
    <div>
      {/*
        map data from API
        <Product /> 
      */}
    </div>
  );
};

export default ProductList;
